---
trigger: manual
---

the design we provide the code should be modular and should be broken into reusable components
also create the api calls using the mock data with the axios instance
and setup the context api for the same 